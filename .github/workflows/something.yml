name: Something

on:
  # push:
  #   branches: [ master ]
  pull_request:

jobs:
  test-release:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Checkout repo
      uses: actions/checkout@v2

    - run: git show --no-patch --format="%P"

    - run: git log
